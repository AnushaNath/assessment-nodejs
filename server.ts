import App from "./app";
const PORT = 3000;

App.listen(PORT, () => {
    console.log('Express server listening on port ' + PORT);
})

/*import app from "./app";
const app = new App();
app.listen();*/